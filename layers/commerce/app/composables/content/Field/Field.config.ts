export enum FieldType {
  TEXT = 'text',
  TEXTAREA = 'textarea',
  SELECT = 'select',
  RADIO = 'radio',
  CHECKBOX = 'checkbox',
  DATE = 'date',
  FILE = 'file',
  BUTTON = 'button',
}

export const DEFAULT_FIELD = FieldType.TEXT

export default DEFAULT_FIELD
