# Layers

Layers extend functionality based on domains.

Use layers when you need to:

- Group domain capabilities such as auth, commerce, social, or shared utilities.
- Add reusable domain features that can be composed into multiple apps.
- Keep business/domain logic modular and independently maintainable.

In short: layers are domain-specific extensions.
