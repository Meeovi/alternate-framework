# Nuxt demo

## Files

- `pages/federation-demo.vue`: page using federation provider registry and sync helpers

## Setup

1. Install dependencies in your app:
   - `npm i @mframework/adapter-federation`
2. Copy the demo file into your Nuxt app.
3. Run `npm run dev`.

## Try

- Visit `/federation-demo`
