# Next.js App Router demo

## Files

- `app/federation-demo/page.tsx`: page that initializes federation providers and renders protocol capabilities

## Setup

1. Install dependencies in your app:
   - `npm i @mframework/adapter-federation`
2. Copy the demo file into your Next.js app.
3. Run `npm run dev`.

## Try

- Visit `/federation-demo`
