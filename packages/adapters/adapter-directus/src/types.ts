// This file defines the DirectusSchema type for consumers of the adapter package.
// You should extend or augment this type in your own app as needed.

export type DirectusSchema = Record<string, any>; // Replace with your schema if needed
