# Adapters

Adapters connect M Framework features to backend systems and external services.

Use adapters when you need to:

- Connect framework modules to APIs, databases, search engines, or protocol servers.
- Normalize backend-specific request/response shapes into framework-friendly contracts.
- Keep backend integration logic isolated from app and theme code.

In short: adapters are the backend connection layer.
