# TanStack Start (React) demo

## Files

- `app/routes/federation-demo.tsx`: route integrating federation providers and timeline merge helpers

## Setup

1. Install dependencies:
   - `npm i @mframework/adapter-federation`
2. Copy files into your TanStack Start React app.
3. Run `npm run dev`.

## Try

- Visit `/federation-demo`
