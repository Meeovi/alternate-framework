# SvelteKit demo

## Files

- `src/routes/federation-demo/+page.svelte`: route showing protocol-agnostic federation outputs

## Setup

1. Install dependencies:
   - `npm i @mframework/adapter-federation`
2. Copy files into your SvelteKit app.
3. Run `npm run dev`.

## Try

- Visit `/federation-demo`
